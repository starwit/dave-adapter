apiVersion: v1
kind: Service
metadata:
  name: {{ include "dave-adapter.fullname" . }}
  labels:
    {{- include "dave-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dave-adapter.selectorLabels" . | nindent 4 }}
